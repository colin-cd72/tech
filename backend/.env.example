# Database
DATABASE_URL=postgresql://user:password@localhost:5432/schedule

# JWT
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server
PORT=3200
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Note: Email and SharePoint settings are configured via the admin UI
# and stored in the database for security
